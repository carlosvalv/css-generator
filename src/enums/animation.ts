 export enum Time {
    SECONDS = 0,
    MILISECONDS = 1
  }
  
export  enum Direction {
    NORMAL = "normal",
    REVERSE = "reverse",
    ALTERNATE = "alternate",
    ALTERNATE_REVERSE = "alternate-reverse"
  }
  
 export enum FillMode {
    NONE = "none",
    FORWARDS = "forwards",
    BACKWARDS = "backwards",
    BOTH = "both"
  }